# 日频回测引擎 — 开发清单

> 更新日期：2026-07-18  
> 标注：**✅ 已完成**　**❌ 未开发**  
> 依据：[日频回测引擎-架构与模块设计.md](./日频回测引擎-架构与模块设计.md)（含 §13 技术选型）  
> 原则：先 BT-1→BT-3 闭环；BT-4 可选；不阻塞项不进 MVP。

配套：

| 文档 | 用途 |
|------|------|
| [开发计划手册.md](./开发计划手册.md) | 全平台 ✅/❌ |
| [开发功能进度.md](./开发功能进度.md) | 叙述看板 |
| 本文 | **回测引擎专项开发清单** |

---

## 0. 阶段总览


| 阶段 | 名称 | 状态 | 说明 |
|------|------|------|------|
| 设计 | 架构 + 模块 + 技术选型 | ✅ | 设计文档已定稿 |
| 前置 | 数据 / DataAPI / 权重读数 | ✅ | M1～M2 已具备，回测只消费 |
| **BT-1** | 撮合内核（规则/账本/单测） | ✅ | `research/backtest/` types/config/rules/ledger/broker |
| **BT-2** | 引擎编排 + DataGate + 落盘 + CLI | ✅ | Lake runs + CLI 已通 |
| **BT-3** | 研究链打通 + 基准 + 文档验收 | ✅ | 基准成分等权 + 操作说明 |
| **BT-4** | 增强（可选） | ❌（B401–408 ✅；余做空占位） | B409 后置 |


---

## 1. 业务前置（已有能力，回测依赖）

> 非回测本包开发项；列出以便依赖清晰。缺任一则 BT-2 实跑受阻。


| # | 模块 / 能力 | 状态 | 路径或说明 |
|---|-------------|------|------------|
| P01 | 全市场日线发布 + `dataset_version` | ✅ | `daily_bar` / publish |
| P02 | 交易日历 | ✅ | `calendar` / `sync_calendar` |
| P03 | DataAPI 行情（含 adjust / version as-of） | ✅ | `api/data_api.py` |
| P04 | 涨跌停字段（质检 HARD 同源） | ✅ | `daily_bar` up/down_limit |
| P05 | 停牌数据 | ✅ | `suspend` + extras |
| P06 | 指数选股域 PIT | ✅ | `index_member` / `get_universe` |
| P07 | 行业 L1/L2 PIT | ✅ | `industry_member` / `get_industry` |
| P08 | 种子因子（mom/vol/turn/ep/bp） | ✅ | `factors` / `factor_value` |
| P09 | 目标权重读数层 | ✅ | `research/portfolio.py` |
| P10 | 行业中性权重 | ✅ | `weight_method=industry_neutral` |
| P11 | 订单级风控语义（对齐参考） | ✅ | `risk`（回测不调用 Gateway） |
| P12 | 日批 / extras 调度 | ✅ | cron + `daily_run --with-extras` |


---

## 2. 设计与工程约定


| # | 项 | 状态 | 说明 |
|---|-----|------|------|
| D01 | 架构与模块设计文档 | ✅ | `docs/日频回测引擎-架构与模块设计.md` |
| D02 | 技术选型冻结（自研内核、零新依赖、Lake 归档） | ✅ | 设计 §13 |
| D03 | 默认假设冻结（next_open / 多头 / 费率） | ✅ | 设计 §2；改默认升 `engine_version` |
| D04 | 包目录 `src/qdata/research/backtest/` | ✅ | 已创建 |
| D05 | `engine_version = bt-daily-0.1` 常量 | ✅ | `config.ENGINE_VERSION` |
| D06 | 开发清单（本文） | ✅ | |


---

## 3. BT-1 — 撮合内核（纯内存 + 单测）

**出口：** 给定人工权重与假行情，能产出 equity / fills；pytest 绿。


| # | 优先级 | 功能模块 | 状态 | 需求要点 | 建议路径 |
|---|--------|----------|------|----------|----------|
| B101 | P0 | `types` 领域对象 | ✅ | `Order` / `Fill` / `Position` / `LedgerSnapshot` / `DailyResult` / `BacktestResult` | `backtest/types.py` |
| B102 | P0 | `BacktestConfig` | ✅ | frozen dataclass；费率/lot/execution/initial_cash；校验 start≤end | `backtest/config.py` |
| B103 | P0 | `FillRules.round_lot` | ✅ | 买卖下取整到手（默认 100） | `backtest/rules.py` |
| B104 | P0 | `FillRules` 滑点 | ✅ | 买加卖减 `slippage_bps` | 同上 |
| B105 | P0 | `FillRules` 费用 | ✅ | 佣金（含最低）+ 卖出印花税 | 同上 |
| B106 | P0 | `FillRules` 可成交性 | ✅ | 停牌；涨停不可买；跌停不可卖 | 同上 |
| B107 | P0 | `PortfolioLedger` | ✅ | cash/shares；`apply_fill`；`nav`/`market_value`/`snapshot` | `backtest/ledger.py` |
| B108 | P0 | `BrokerSim.rebalance_to_weights` | ✅ | 目标权重→目标股数；先卖后买；拒单不抛错；现金不足高权重优先 | `backtest/broker.py` |
| B109 | P0 | 单测：规则与费用 | ✅ | lot/fee/slippage 边界 | `tests/research/test_backtest_rules.py` |
| B110 | P0 | 单测：涨停拒买 / 停牌 | ✅ | `rejected_reason` 非空；持仓或现金符合预期 | `tests/research/test_backtest_broker.py` |
| B111 | P0 | 单测：全现金无信号 | ✅ | nav 恒等于 initial_cash | 同上 |


**BT-1 验收：** ✅ `pytest tests/research/test_backtest_*.py` 13 passed (2026-07-18)  

```text
pytest tests/research/test_backtest_*.py -q
```

---

## 4. BT-2 — 引擎编排 + 读数 + 落盘 + CLI

**出口：** CLI 对真实区间跑通，Lake 落下 `run_id`。


| # | 优先级 | 功能模块 | 状态 | 需求要点 | 建议路径 |
|---|--------|----------|------|----------|----------|
| B201 | P0 | `DataGate` | ✅ | 包装 DataAPI；批量拉 open/close/limit；进程内缓存；强制 version | `backtest/data_gate.py` |
| B202 | P0 | DataGate 停牌查询 | ✅ | 对接 suspend 或等价判定 | 同上 |
| B203 | P0 | `FromWeightFrame` 信号源 | ✅ | 实现 `WeightSignalProvider`；按日取权重 | `backtest/signals.py` |
| B204 | P0 | `BacktestEngine` 日循环 | ✅ | pending 调仓 → 成交日撮合 → 收盘估值 → 收盘登记次日信号 | `backtest/engine.py` |
| B205 | P0 | 防前视：末日信号丢弃 | ✅ | 无下一交易日则 warning + 不成交 | 同上 |
| B206 | P0 | `metrics` 基础集 | ✅ | total/ann return、vol、sharpe、maxDD、turnover、avg_cash、hit_ratio | `backtest/metrics.py` |
| B207 | P0 | `RunStore` Lake 归档 | ✅ | `meta.json` + equity/fills parquet + metrics.json | `backtest/store.py` |
| B208 | P0 | meta 强制字段 | ✅ | engine/dataset/factor version、execution、price_model、config | 同上 |
| B209 | P0 | CLI `run_backtest` | ✅ | argparse；打印 metrics + run_id 路径 | `jobs/run_backtest.py` |
| B210 | P0 | 包导出 `__init__` | ✅ | Engine/Config/FromWeightFrame/run | `backtest/__init__.py` |
| B211 | P0 | 单测：防前视 | ✅ | 构造末日信号无 fill | `tests/research/test_backtest_no_lookahead.py` |
| B212 | P0 | 单测：双跑复现 | ✅ | 同配置 metrics 一致（容差） | `tests/research/test_backtest_repro.py` |
| B213 | P1 | 集成烟测（需 CH） | ✅ | `@pytest.mark.integration`；短区间 mom_20 | 可选 |


**BT-2 验收命令（目标）：** ✅（2026-07-18 CLI 实跑 mom_20 top20）  

```bash
.venv/bin/python -m qdata.jobs.run_backtest \
  --start 2026-04-01 --end 2026-07-15 \
  --universe 000905.SH --factor mom_20 --top-n 50 \
  --version 2026-07-15 --execution next_open
```

---

## 5. BT-3 — 研究链打通 + 基准 + 文档验收

**出口：** 一条命令从 `RebalanceSpec` 到可对比指标；手册可引用。


| # | 优先级 | 功能模块 | 状态 | 需求要点 | 建议路径 |
|---|--------|----------|------|----------|----------|
| B301 | P0 | `FromRebalanceSpec` | ✅ | 内部调用 `build_weight_series` | `backtest/signals.py` |
| B302 | P0 | CLI 支持 RebalanceSpec 参数 | ✅ | universe/factor/top_n/weight_method/industry_level | `jobs/run_backtest.py` |
| B303 | P0 | 基准收益与超额指标 | ✅ | `benchmark` 缺数据则跳过并 meta 标记 | `data_gate` + `metrics` |
| B304 | P1 | 拒单统计进 metrics/meta | ✅ | n_rejects / 按 reason 计数 | `engine` / `metrics` |
| B305 | P1 | `industry_neutral` 端到端样例 | ✅ | 文档命令 + 一次实跑记录 | docs 示例 |
| B306 | P0 | 操作说明增补回测章节 | ✅ | `数据采集操作说明` 或独立「回测操作」小节 | docs |
| B307 | P0 | 开发计划手册挂接 M2.x/BT | ✅ | 手册已增 M2.5 与本文链接 | `开发计划手册.md` |
| B308 | P1 | 进度看板同步 | ✅ | 进度板已增 M2.5 行 | `开发功能进度.md` |


**BT-3 验收：** ✅（2026-07-18：excess + industry_neutral + §9.3.1）  

---

## 6. BT-4 — 增强（可选，非 MVP）


| # | 优先级 | 功能模块 | 状态 | 需求要点 |
|---|--------|----------|------|----------|
| B401 | P2 | ADV/成交额占比约束 | ✅ | 单票成交不超过 ADV 某比例 |
| B402 | P2 | `execution=next_close` | ✅ | 配置已预留枚举，实现第二路径 |
| B403 | P2 | CH `backtest_run` / `backtest_equity` | ✅ | 多 run 对比查询 |
| B404 | P2 | Tearsheet HTML（quantstats optional） | ✅ | `optional-dependencies.tearsheet` |
| B405 | P2 | 每日持仓面板落盘（稠密） | ✅ | 现 v1 可只存调仓日/稀疏 |
| B406 | P2 | 研究 API 查询 run | ✅ | CLI `list_backtests`（非 FastAPI BFF） |
| B407 | P2 | Dashboard 回测报告页 | ✅ | Streamlit：`jobs/backtest_dashboard`；`pip install -e '.[dashboard]'` |
| B408 | P3 | 与 Paper 共享 `CostModel` | ✅ | `trading/cost.py`；回测 `cfg.cost_model()` / Paper `cost=`；运行时隔离 |
| B409 | P3 | 做空 / 融资 | ❌ | 设计非目标，仅占位 |


---

## 7. 明确不做（本阶段保持 ❌，且不排期）


| # | 项 | 状态 | 说明 |
|---|-----|------|------|
| X01 | 分钟/Tick 撮合 | ❌ | 非目标 |
| X02 | 完整 Brinson/Barra 归因 | ❌ | 仅留扩展点 |
| X03 | 回测内调用 TradingGateway | ❌ | 禁止污染 Paper |
| X04 | Backtrader/Zipline/vectorbt 内核 | ❌ | 选型否决 |
| X05 | 真金实盘（4.4） | ❌ | 仍属 M3，与回测并行无关 |


---

## 8. 统计


| 范围 | ✅ | ❌ |
|------|----|----|
| 设计与清单 D0x | 6 | 0 |
| 业务前置 P0x | 12 | 0 |
| BT-1 | 11 | 0 |
| BT-2 | 13 | 0 |
| BT-3 | 8 | 0 |
| BT-4 可选 | 8 | 1 |
| 明确不做 X0x | 0 | 5（刻意不开发） |


> MVP（BT-1～3）✅；BT-4：ADV/next_close/持仓/CH/tearsheet/Dashboard/CostModel ✅。余做空占位（B409）。

---

## 9. 建议开发顺序（只盯 ❌）

```
BT-1: B101 → B102 → B103..B106 → B107 → B108 → B109..B111
  → BT-2: B201 → B203 → B204 → B206 → B207 → B209 → B211/B212
  → BT-3: B301 → B302 → B303 → B306/B307
  → （可选）BT-4
```

**下一手：** 策略实验对比；**4.4 真金仍暂缓**；B409 不做空/融资不排期。

---

## 10. 变更规则

- 完成某项：将本文对应行 ❌→✅，并在备注写 PR/日期或验收摘录。  
- 改默认成交/复权/缩减策略：升 `engine_version`，并改设计 §2。  
- 不把 X0x「明确不做」改成排期，除非修订非目标章节。
