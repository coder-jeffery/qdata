# M3：开闸验收（真金 4.4 闸门）

> **默认状态：闸门关闭。**  
> 纸交易（4.1～4.3）已通；**真金实盘（4.4）在本文件全部硬条件勾选并通过「开闸授权」前禁止启用。**  
> 本文件只定义闸门与验收证据，不包含真金下单实现。

相关：手册 [开发计划手册.md](./开发计划手册.md) §4 · [量化平台开发手册.md](./量化平台开发手册.md) §7 · Web BFF [`src/qdata/api/bff.py`](../src/qdata/api/bff.py)

---

## 0. 范围与禁止项

| 允许（闸门关闭期间） | 禁止（未开闸） |
|----------------------|----------------|
| Paper 调仓 / 盯市 / 对比 | `QDATA_BROKER=miniqmt` / `easytrader` 真下单 |
| Lake `realtime` 叠加（A505 ✅） | Web / Dashboard 暴露「实盘」账户或下单按钮 |
| 通道骨架代码只读演练（connect 探活，不发单） | 自动把日批/信号接到真金 broker |
| 文档与验收脚本 | 绕过本清单的「临时试一把」 |

开闸后仍须：**单笔限额、人工二次确认、可一键切回 `paper`。**

---

## 1. 硬条件清单（全部 ✅ 才可讨论开闸）

### G1 — 纸交易闭环可复现

| # | 条件 | 证据 | 状态 |
|---|------|------|------|
| G1.1 | `paper_demo` 整手成交 / 非整手下调 / 非法拒单路径可复现 | CLI 日志或 session `rejects` | ☐ |
| G1.2 | 实验挂钩 → 信号 → Paper 可审计（A308） | session `meta.from_experiment` | ☐ |
| G1.3 | 日终盯市可落盘且可对比（A306 / A504） | `mark_latest` + `compare_sessions` | ☐ |

```bash
.venv/bin/python -m qdata.jobs.paper_demo --code 600000.SH --side buy --qty 100
.venv/bin/python -m qdata.jobs.paper_demo --code 600000.SH --side buy --qty 150
.venv/bin/python -m qdata.jobs.paper_demo --code 600000.SH --side buy --qty 50
```

### G2 — Paper 多日演练

| # | 条件 | 证据 | 状态 |
|---|------|------|------|
| G2.1 | **同一 session** 连续 ≥ **5** 个交易日完成盯市 | marks 序列日期连续（交易日历） | ☐ |
| G2.2 | 演练期无未解释拒单激增（相对成交） | rejects / fills 摘要 | ☐ |
| G2.3 | Web 或 Dashboard 能打开该 session 持仓/成交/盯市 | `/paper` 或 Streamlit Paper 页截图/笔记 | ☐ |

### G3 — 研究可信度（与 M2.8 DD4 对齐）

| # | 条件 | 证据 | 状态 |
|---|------|------|------|
| G3.1 | 研究区间日线纵深满足当前 DD1 验收（≥1 年或项目书面降级说明） | DD1 验收记录 | ☐ |
| G3.2 | 因子覆盖对齐后重跑实验矩阵（DD3/DD4） | experiment summary 落盘 | ☐ |
| G3.3 | 最优 cell → Paper 再盯市至少 1 轮 | session + mark | ☐ |

> G3 未完成：**不得**以「代码齐了」为由开 4.4。可继续只做 Paper。

### G4 — 行情与风控

| # | 条件 | 证据 | 状态 |
|---|------|------|------|
| G4.1 | realtime 快照可写入并读取（4.1） | `lake/realtime/.../data.parquet` | ☐ |
| G4.2 | A505：交易台/研判可叠加最新快照（Web） | `/api/realtime/quotes` + UI | ✅ |
| G4.3 | 风控规则仍生效：整手 / 涨跌停 / 停牌 / 资金·持仓限额 | `risk.check_order` + paper_demo | ☐ |

### G5 — 运维与告警

| # | 条件 | 证据 | 状态 |
|---|------|------|------|
| G5.1 | 日批软挂因子监控可用（A405） | `factor_monitor` + webhook 区分 WARN/FAIL | ☐ |
| G5.2 | Web 告警中心能聚合监控 / 失败 job / 健康软告警 | `GET /api/alerts` | ✅（代码）/ 运维确认 ☐ |

### G6 — 开闸授权（流程闸）

| # | 条件 | 证据 | 状态 |
|---|------|------|------|
| G6.1 | 负责人书面确认：通道（miniqmt **或** easytrader）、资金账号、单笔/日累计上限 | 本节「开闸记录」填表 | ☐ |
| G6.2 | 默认 `QDATA_BROKER=paper`；真金仅显式环境变量/密钥切换 | `.env` 审查 | ☐ |
| G6.3 | 回滚预案：断连真金、切回 paper、冻结 Web 写入口的步骤已写明 | 本节 §3 | ☐ |
| G6.4 | **显式授权句**已签署（见下） | 日期 + 签名/记录 | ☐ |

**授权句（须原样抄写到开闸记录）：**

> 我确认 G1～G5 已满足，知悉真金下单不可撤销风险，批准在指定通道与限额内启用 4.4；未批准前任何真金下单视为违规。

---

## 2. 软条件（开闸加分，不单独放行）

| # | 项 | 说明 |
|---|-----|------|
| S1 | 连续 5 日 `daily_run` 成功 | 对齐 M1.5 无人值守口径 |
| S2 | Paper 演练 ≥ 10 个交易日 | 严于 G2.1 |
| S3 | 真金通道 **connect 探活**（不下单） | 仅验证本机 QMT/客户端可达 |
| S4 | 异步任务失败可在 `/ops/jobs` 追溯 | Web P2 |

---

## 3. 回滚预案（开闸后必须可执行）

1. 立即将 `QDATA_BROKER=paper`（或卸掉真金凭据环境变量）并重启 BFF / 作业进程。  
2. 停止一切会调用 `get_broker()` 且非 paper 的 cron / 手动 job。  
3. Web：确认无实盘下单路由（当前设计：**真金 UI 不在运营后台范围**）。  
4. 保留最后成功 Paper / 真金前 session 与日志，供复盘。

---

## 4. 开闸记录（空白表）

| 字段 | 填写 |
|------|------|
| 日期 | |
| 申请人 | |
| 审批人 | |
| 通道 | miniqmt / easytrader |
| 账号标识（可脱敏） | |
| 单笔上限 | |
| 日累计上限 | |
| G1～G5 勾选附件链接 | |
| 授权句已抄写 | ☐ |
| 回滚演练日期 | |

**开闸结论：** ☐ 仍关闭 · ☐ 有条件开启（限额内） · ☐ 拒绝

---

## 5. 与代码现状对照（2026-07-19）

| 项 | 状态 |
|----|------|
| 4.1 实时总线 | ✅ |
| 4.2 Gateway + Paper | ✅ |
| 4.3 风控 | ✅ |
| A505 Web 实时叠加 | ✅ `GET /api/realtime/quotes` · Trade/Judgment UI |
| 4.4 真金 | ❌ **闸门关闭**（本文件） |
| miniqmt / easytrader 骨架 | 代码存在；**禁止默认启用** |

---

## 6. 变更规则

- 勾选硬条件：在对应行改 ☐→✅，并留下证据路径（Lake session、日志、PR）。  
- **不得**因 A505 或 Web P2 完成而自动开 4.4。  
- 一旦开闸：在 [开发计划手册.md](./开发计划手册.md) §4.4 与 [开发功能进度.md](./开发功能进度.md) 同步状态，并保留本节开闸记录。
